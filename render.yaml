services:
  - type: web
    name: convertisseur-unites
    runtime: node
    plan: free
    # Le déploiement est déclenché par GitHub Actions (API Render) après la réussite des tests.
    autoDeployTrigger: "off"
    buildCommand: npm ci --include=dev && npm run check
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: "24"
      - key: HOST
        value: 0.0.0.0
